/*
 * AEP parser entry point.
 * Includes the glaxnimate-derived parser headers so the parsing code
 * is compiled into the friction_aep_parser static library (C++17).
 *
 * SPDX-License-Identifier: GPL-3.0-or-later
 */
#include "aep_parser.hpp"
