name: Windows

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: windows-2022
    steps:
    - uses: actions/checkout@v4
    - name: Build
      run: |
        echo "Windows build needs proper Qt and FFmpeg setup"
        echo "Skipping for now"
    - name: Archive build
      uses: actions/upload-artifact@v4
      with:
        name: friction-windows-x64
        path: build/
